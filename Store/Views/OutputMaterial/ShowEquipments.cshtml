@model List<Store.Models.Stores.StoreRoom.OutputHasEquipmentModel>

@{
    ViewBag.Title = "  تجهیزات و تعداد تجهیزات خروجی از انبار ";
    Layout = null;

}
<div class="row bg-title">
    <div class="col-lg-3 col-md-4 col-12">
        <h4 class="page-title">نمایش لیست تجهیزات خروجی  </h4>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="white-box">
            <div class="table-responsive">
                <div id="example_wrapper" class="dataTables_wrapper">

                    <table id="EquipmentsGrid" class="table table-striped table-condensed table-bordered no-margin nowrap">
                        <thead>
                            <tr>
                                <th class="text-center">عنوان تجهیز</th>
                                <th class="text-center">تعداد تجهیز</th>
                                <th class="text-center">تاریخ خروج</th>
                                <th class="text-center">کاربر ایجاد کننده</th>
                                <th class="text-center">تاریخ ایجاد </th>
                                @*<th>آخرین کاربر ویرایش کننده</th>
                                <th>تاریخ آخرین ویرایش </th>*@
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="text-center">@item.Title</td>
                                    <td class="text-center">@item.Count</td>
                                    <td class="text-center">@item.ExitDate</td>
                                    <td class="text-center">@item.ActionUserName</td>
                                    <td class="text-center">@item.ShamsiCreateDate</td>
                                    @*<td>@item.EditUserName</td>
                                    <td>@item.ShamsiEditDate</td>*@
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>


